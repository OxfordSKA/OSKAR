TODO:

*) Create a numpy array like interface to oskar_Mem.
    e.g.
        m = oskar.mem([1,2,3,4,5,6], location=gpu, type=double)
        print m.location()
        print m.type()
        print m.shape()
        print m.data()
        m.resize((100,))
        m2 = oskar.zeros((200,), location=cpu, type=double_complex)

*) Extend the oskar_Mem concept to a few other types.
    - Visibilities?

*) Interface to oskar binary file type

*) Wrap modules in an oskar class
    e.g.
        import oskar
        oskar.image.make(...)
    -or-
        from oskar import image
        image.make(...)
